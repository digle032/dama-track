<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Add Shipment</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <h1 class="mb-4 text-center">Add Shipment</h1>

  <% if (error) { %>
    <div class="alert alert-danger"><%= error %></div>
  <% } %>

  <form method="POST" action="/shipments/add">
    <div class="mb-3">
      <label for="tracking" class="form-label">Tracking Number (scan here)</label>
      <input type="text" class="form-control" id="tracking" name="tracking" required autofocus value="<%= shipment.tracking || '' %>">
    </div>

    <div class="mb-3">
      <label for="date" class="form-label">Date</label>
      <input type="date" class="form-control" id="date" name="date" required value="<%= shipment.date || '' %>">
    </div>

    <div class="mb-3">
      <label for="location" class="form-label">Location</label>
      <input type="text" class="form-control" id="location" name="location" required value="<%= shipment.location || '' %>">
    </div>

    <div class="mb-3">
      <label for="client" class="form-label">Client</label>
      <input type="text" class="form-control" id="client" name="client" required value="<%= shipment.client || '' %>">
    </div>

    <div class="mb-3">
      <label for="transport" class="form-label">Transport</label>
      <input type="text" class="form-control" id="transport" name="transport" required value="<%= shipment.transport || '' %>">
    </div>

    <div class="mb-3">
      <label for="courier" class="form-label">Courier</label>
      <input type="text" class="form-control" id="courier" name="courier" required value="<%= shipment.courier || '' %>">
    </div>

    <div class="mb-3">
      <label for="status" class="form-label">Status (optional)</label>
      <input type="text" class="form-control" id="status" name="status" value="<%= shipment.status || '' %>">
    </div>

    <div class="text-end">
      <button type="submit" class="btn btn-primary">Add Shipment</button>
      <a href="/shipments" class="btn btn-secondary">Cancel</a>
    </div>
  </form>
</body>
</html>
